name: comp
description: composite equivalent of the reusable workflow
inputs:
  tag:
    required: true
  tok:
    required: false
    default: ''
runs:
  using: composite
  steps:
    - name: Echo
      shell: bash
      run: echo "tag=${{ inputs.tag }}"
